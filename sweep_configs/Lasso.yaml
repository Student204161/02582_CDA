program: main.py  # Assuming your main script is main.py
name: LassoSweep  # Give your sweep a descriptive name
method: bayes
metric:
  goal: minimize
  name: root_mean_mse
parameters:
  alpha:
    min: 0.000001
    max: 1.0
    distribution: log_uniform_values